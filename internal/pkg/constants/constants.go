package constants

const (
	// BotTokenHeader is the key name in the context for BOT Token.
	BotTokenHeader = "Token"
	// GHPATHeader is the key name in the context for Github Personal Access Token.
	GHPATHeader = "PAT"
	// This is the key for reading the owner name from env var.
	OwnerKey = "GithubOwner"
)
